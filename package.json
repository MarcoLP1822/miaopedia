{
  "name": "miaopedia",
  "private": true,
  "scripts": {
    "check": "node --check dist/app.js && node --check dist/album.js && node --check dist/cats.js",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
